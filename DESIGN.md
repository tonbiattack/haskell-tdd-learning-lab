# 設計判断

この教材では、外部境界を除くロジックを純粋関数に置き、テストの入力と出力を明示します。`app/Main.hs` は表示だけを担当し、計算は `HaskellTdd.PureIO` に切り出します。これにより、IOを実行しなくても業務ロジックをHspecで検証できます。

`findUser` は見つからない状態を例外や特別な値ではなく `Maybe User` で表現します。`validateRegistration` は単一エラーだけを返すのではなく、入力を一度検査して `[ValidationError]` を返します。複数の不正条件を同時に示せるため、入力検証の学習に適しています。

`UserId` は `newtype` で包み、裸の `Int` と識別子を区別します。年齢のような制約付き値は、この入門版では検証関数で扱い、次の発展課題としてスマートコンストラクタへの移行を残します。短いコードを書くことより、型と関数の責務が読み取れることを優先します。

教材のTDD履歴では、完成コードだけでなく、意図的に未定義の関数を置いたRed段階、最小実装のGreen段階、`map`・`fold`・パターンマッチへ整理したRefactor段階を別コミットで保持します。
